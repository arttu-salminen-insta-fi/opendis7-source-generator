
    // autogenerated using string template dislargeenumpart2.txt

    /** Provide enumeration value
     * @return integer value
     */
    int getValue();

    /** Provide enumeration description
     * @return description
     */
    String getDescription();

    /** bit width for this enumeration
     * @return number of bits wide
     */
    static int getEnumBitWidth() {
        return %s;
    }

    /**
     * Provide enumeration for a given value
     * @param value integer value of interest
     * @return enumeration corresponding to numeric value
     */
    static %s getEnumForValue(int value) {
        int[] lastValues = {%s};
        Function<Integer, %s>[] enumGetters = new Function[] { %s };

        for (int i = 0; i < lastValues.length; i++) {
            if (value <= lastValues[i]) {
                return enumGetters[i].apply(value);
            }
        }
        System.out.flush(); // ensure contiguous console outputs
        System.err.println("No corresponding enumeration found for value " + value + " of enum %s");
        System.err.flush(); // ensure contiguous console outputs
        return null;
    }

    /**
     *  Implemented to enable equivalent default value initialization with normal enum classes
     */
    static %s[] values() {
        return Stream.of(%s).flatMap(Stream::of).toArray(%s[]::new);
    }

