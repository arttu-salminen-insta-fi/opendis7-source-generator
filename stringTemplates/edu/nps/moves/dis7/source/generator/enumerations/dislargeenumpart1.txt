// autogenerated using string template dislargeenumpart1.txt

package %s;

import java.nio.ByteBuffer;
import java.io.DataInputStream;
import java.io.DataOutputStream;
import java.io.IOException;

import java.util.function.Function;
import java.util.stream.Stream;

import edu.nps.moves.dis7.pdus.*;

/**
 * This <code>interface</code> is used as a unifier for multiple <code>enum</code> classes, as the enumeration is too
 * large for one <code>enum</code>.
 * https://stackoverflow.com/questions/11883043/does-an-enum-class-containing-20001-enum-constants-hit-any-limit
 *
 * The used <code>enum</code> classes are generated from XML.
 * %s %s,
 * marshal size %s;
 * %s has %d enumerations total.
 */
public interface %s %s
{